# Public, same-origin API paths only. Do not place provider or email secrets in Vite variables.
# Keep both values blank until the matching server routes, provider accounts,
# storage, and end-to-end tests are approved for production.
VITE_REQUEST_ENDPOINT=
VITE_INVOICE_LOOKUP_ENDPOINT=
